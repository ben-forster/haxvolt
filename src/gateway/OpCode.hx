package haxvolt.gateway;

class OpCode
{
    public static function checkOpCode(op:Int)
        {
            var opcode:String = "";
            switch (op)
            {
                case 0:
                    opcode = ""
            }
        }
}